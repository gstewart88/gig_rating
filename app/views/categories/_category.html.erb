<div class="box">
<%= category.name %>
<%= link_to("Show", category_path(category)) %>
<%= button_to('Edit', edit_category_path(category), method: :get) if can? :manage, Category %>
<%= button_to('Delete', category_path(category), method: :delete, data:{confirm: 'Delete the category? Are you sure?'}) if can? :delete, Category %>
</div>
<br>