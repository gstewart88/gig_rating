<p><strong>Show Name: </strong><%= show_review.show.try(:name) %></p>
<p><strong>Review Title: </strong><%= show_review.title %></p>


<p><strong>Show Average Rating: </strong><%= "★"*show_review.rating %><%= "☆"*(5 - show_review.rating) %></p>

<div class="menu">
  <ul>
    <li><%= button_to('Show', show_review, method: :get) %></li>
    <li><%= button_to('Edit', edit_show_review_path(show_review), method: :get) if can? :manage, ShowReview %></li>
    <li><%= button_to('Delete', show_review_path(show_review), method: :delete, data:{confirm: 'Delete the show_review? Are you sure?'}) if can? :delete, ShowReview %></li>
  </ul>
</div>